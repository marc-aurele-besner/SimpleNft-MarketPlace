
// buy / sell 

it("Does user can buy NFT (trade eth for token)(should be)") {
    
};

it("Does user can sell NFT (trade eth for token)(should be)") {
    
};

// WithDrawEth/ Send

it("Does user can withDrawEth from the marketplace (should be)"){

};

it("Does user can sendEth to the marketplace (should  be)"){

};


//blacklist

it("can a blacklisted user still withdrawEth (should not)"){

};

it("can a blacklisted user still sell NFT (should not)"){

};

it("can a blacklisted user still buy NFT (should not)"){

};

it("can a blacklisted user still sendEth (should not)"){

};

it("can a blakclisted user still create a collection (should not)")

//admin

it("is the Admin able to withdrawAll Eth sent to the marketplace (should be)"){

};

it("is the Admin able to add new moderator (should be)"){

};

it("is the Admin able to remove moderator (should be)"){

};

it("is the ownerShip changeable"){

};

// admin / moderator

it("is the moderator able to change ownerShip (should not)"){

};

it("is the moderator able to withdrawAll eth(should not)"){

};



// admin / user

it("is the user1 able to change ownerShip (should not)"){

};

it("is the user2 able to withdrawAll eth(should not)"){

};


